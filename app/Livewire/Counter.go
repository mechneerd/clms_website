package livewire

// Counter is a simple Livewire-style reactive component example.
// This is currently disabled because the GoW framework's livewire package
// does not yet export BaseComponent, MarkDirty, or GetID.
//
// To enable this, the GoW framework needs to provide:
//   - BaseComponent struct (with ID, MarkDirty, GetID methods)
//   - Livewire HTTP handler endpoint
//
// For now, the welcome page loads /livewire/counter via JS fetch.
